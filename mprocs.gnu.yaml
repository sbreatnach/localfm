procs:
  postgres:
    shell: "/usr/lib/postgresql/17/bin/postgres -p 5532 -k $(pwd)/deployment/local -D ./deployment/local/pgdata"
    stop: "SIGTERM"
  caddy:
    cmd: ["caddy", "run", "--config", "./deployment/local/Caddyfile"]
    autostart: false
  nats:
    cmd: ["nats-server", "-c", "./deployment/local/jetstream.conf"]
    autostart: false
  localfm:
    cmd: ["uv", "run", "python", "-m", "manage", "runserver", "8001"]
    stop: "SIGTERM"
